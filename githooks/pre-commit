#!/bin/sh

if terraform-docs --version | grep "zsh: command not found: terraform-docs" ; then brew install terraform-docs; else terraform-docs --version; fi;
terraform-docs markdown table --output-file README.md --output-mode inject .
git add README.md && git commit --no-verify -m "Updated README.md file"